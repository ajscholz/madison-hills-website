import styled from 'styled-components';

export default styled.h1`
  font-size: 2.25rem;
  font-weight: bold;
  color: var(--white);
  text-align: center;
  padding: 5vw;
  max-width: 700px;
  @media (min-width: 400px) {
    font-size: 3rem;
  }
`;
